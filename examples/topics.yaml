# ghrc plan --name shotat/ghrc -f examples/topics.yaml
# NOTE: This config updates only topics. Other attributes like `homepage`, `description` are just ignored.
spec:
  repo:
    topics:
    - git
    - github
    - go
    - cli
    description: null # ignored.
